/**
 * Identity Service
 * 
 * Handles A-Pass (KYC) operations for all participants.
 * Each participant brings their own wallet - we only verify their A-Pass status.
 */

export * from './schemas';
export * from './identity.service';
